Create Table products(
    id integer primary key,
    cost real,
    category text,
    name text,
    brand text,
    reatail_price real,
    department text,
    sku text,
    distribution_center_id integer
);